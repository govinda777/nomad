{
  "datacenter": "dc-gcp",
  "data_dir": "/consul/data",
  "log_level": "INFO",
  "server": true,
  "bootstrap_expect": 1,
  "ui_config": {
    "enabled": true
  },
  "bind_addr": "0.0.0.0",
  "client_addr": "0.0.0.0",
  "connect": {
    "enabled": true
  },
  "ports": {
    "grpc_port": 8502
  }
  // TODO: Configure the WAN join address to connect with dc-aws
  // Hint: Add the "retry_join_wan" array pointing to ["consul-aws"]
}
